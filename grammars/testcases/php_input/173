<?php
function foo($a)
{
   $a=5;
   try {
       echo func_get_arg(-1);
   } catch (\Error $e) {
       echo $e->getMessage() . \PHP_EOL;
   }

   try {
       echo func_get_arg(2);
   } catch (\Error $e) {
       echo $e->getMessage() . \PHP_EOL;
   }
}
foo(2);