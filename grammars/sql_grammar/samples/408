CREATE TABLE v0 ( v1 INTEGER PRIMARY KEY CHECK( v1 ) ) ; INSERT INTO v0 VALUES ( 3 ) ; SELECT DISTINCT v1 , CASE WHEN v1 = 12 THEN 'Brand#23' ELSE v1 END AS col1 FROM v0 WHERE v1 IN ( 'PROMO%%' , 'X' , 'Ar' ) ; 
